# Google Analytics 4 — optional override (default in src/lib/analytics.ts is G-BJCKN12FND)
# Set on the host (e.g. Render) if you want a different property without changing code.
VITE_GA_MEASUREMENT_ID=G-BJCKN12FND
